/** Zoom level at which a hovered node begins its enter-cue (sweep ring). */
export const ZOOM_FOCUS_THRESHOLD = 0.9;

/** Milliseconds to hold hover before focus mode activates. */
export const FOCUS_DWELL_MS = 600;
